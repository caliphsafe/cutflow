import { PaymentConnections } from "@/components/payments/PaymentConnections";
export default function ConnectionsPage(){return <div className="dashboard-page connections-page"><header className="page-header"><div><p className="eyebrow">PAYMENT SERVICES</p><h1>Connections</h1><p>Let each barber connect the payment company they already trust by signing in and approving CutFlow.</p></div></header><PaymentConnections/></div>}
